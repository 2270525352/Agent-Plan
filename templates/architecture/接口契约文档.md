# 接口契约文档

## 文档规则

- 字段级接口以本文档为准。
- 未写入本文档的协议变更不能默认视为允许。
- 实现代理不得另起协议。

## 接口列表

### API-0001

接口 ID：API-0001

路径或入口：

调用方：

被调用方：

请求字段：

```json
{}
```

响应字段：

```json
{}
```

兼容性要求：

禁止变更：

验收样本：

失败处理：

关联任务：

